unit URecord;

interface

type
Direcciones = Record
    izquierda : Boolean;
    derecha : Boolean;
    superior : Boolean;
    inferior : Boolean;
 End;

 Direction = (izquierda,derecha, superior, inferior );

implementation

end.
